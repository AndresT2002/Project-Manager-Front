enum Role {
  ADMIN = "admin",
  USER = "user",
  LEADER = "leader",
}
enum Pages {
  LOGIN = "/login",
  REGISTER = "/register",
  DASHBOARD = "/dashboard",
  ADMIN = "/admin",
  UNAUTHORIZED = "/unauthorized",
  HOME = "/",
}

export { Role, Pages };
